import { defineConfig } from 'astro/config';

export default defineConfig({
  // Set `site` later when you know the canonical production URL.
  // site: 'https://example.com',
  trailingSlash: 'always'
});
